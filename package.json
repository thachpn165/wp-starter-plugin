{
  "name": "my-plugin",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite build --watch",
    "dev:sync": "./scripts/dev-sync.sh",
    "dev:docker": "npm run build && ./scripts/dev-sync.sh",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sass": "^1.69.0",
    "vite": "^5.0.0"
  }
}
